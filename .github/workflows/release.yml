name: Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  release:
    if: github.event.workflow_run.conclusion == 'success'
    uses: Tech-S1/ci-templates/.github/workflows/go-release.yml@v0.2.0
    with:
      ref: ${{ github.event.workflow_run.head_sha }}
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
