name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "renovate.json"
  pull_request:
    paths-ignore:
      - "README.md"
      - "renovate.json"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  go:
    uses: Tech-S1/ci-templates/.github/workflows/go-ci.yml@ee9c56d0ed5c68d91e4f67cfa27cb884444edfff # v0.2.2
    with:
      build-packages: .
