name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "renovate.json"
  pull_request:
    paths-ignore:
      - "README.md"
      - "renovate.json"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  go:
    uses: Tech-S1/ci-templates/.github/workflows/go-ci.yml@59d1ba4531f379719fb8f5c4e14fb8f097ece7bb # v0.2.1
    with:
      build-packages: .
