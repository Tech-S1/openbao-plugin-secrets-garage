name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
      - "renovate.json"
  pull_request:
    paths-ignore:
      - "README.md"
      - "renovate.json"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  go:
    uses: Tech-S1/ci-templates/.github/workflows/go-ci.yml@6c3ef4fdae983ba63dee2446d0f9435bbbd69d82 # main
    with:
      build-packages: .
